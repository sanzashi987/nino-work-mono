// system action reserved and not allowed to be exported
export const EXCLUDED_ACTIONS = new Map<string, string>([
  // ['updateConfig', 'updateConfig']
]);

export const EXCLUDED_EVENTS = new Map<string, string>([]);
